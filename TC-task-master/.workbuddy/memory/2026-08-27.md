# 2026-08-27

- 项目本地启动（Docker 不可用，走本地方式）：
  - 后端：`agent-backend/.venv`（系统 Python 3.9，已验证兼容），依赖已装；`.env` 已从 example 生成并填入用户提供的 DEEPSEEK_API_KEY。
  - 后端启动方式（关键坑：Bash 工作目录每次重置到项目根，相对路径/cd 易失败）：用绝对路径 uvicorn —— `/Users/user/Desktop/TC-task-master/agent-backend/.venv/bin/uvicorn app.main:app --app-dir /Users/user/Desktop/TC-task-master/agent-backend --host 0.0.0.0 --port 8000`（后台任务 tBYYmW）。
  - 前端：pnpm 未安装，用 `npx pnpm --dir ... install`；esbuild 构建脚本被 pnpm 安全策略忽略（ERR_PNPM_IGNORED_BUILDS，exit 1），但 @esbuild/darwin-arm64 二进制实际存在，不影响 vite 运行。pnpm run dev 会因预检跑 install 而失败，绕开方式：直接 `node .../node_modules/vite/bin/vite.js serve <frontend目录> --port 5173`（后台任务 FrbVXu）。
  - 新增 `frontend/pnpm-workspace.yaml`（onlyBuiltDependencies: esbuild）。
- 启动后验证：后端 /api/health 返回 ok（deepseek-chat，api_key_configured=true）；前端 5173 HTTP 200，/api 代理转发正常。
